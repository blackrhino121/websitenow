<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="<%= images.logo %>">
    
    <%- include('partials/seo-meta', {
        title: title,
        metaDescription: 'Rivix Servers operates from our primary datacenter in Ashburn, Virginia (US-East). Low latency, high performance infrastructure for North American players.',
        canonicalUrl: typeof canonicalUrl !== 'undefined' ? canonicalUrl : (typeof BASE_URL !== 'undefined' ? BASE_URL : 'https://rivixservers.com') + '/locations',
        ogImage: (typeof BASE_URL !== 'undefined' ? BASE_URL : 'https://rivixservers.com') + (typeof images !== 'undefined' && images.hero ? images.hero[0] : '/images/hero (1).png'),
        ogType: 'website'
    }) %>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://unpkg.com/aos@2.3.1/dist/aos.css">
    
    <style>
        /* Base Dark Mode Setup */
        .bg-dark-bg { background-color: #0F172A; }
        .bg-dark-card { background-color: #1E293B; }
        .text-rivix-orange { color: #FF6B00; }
        .text-rivix-green { color: #22C55E; }
        .text-rivix-blue { color: #2563EB; }
        
        body { padding-top: 72px; background-color: #0F172A; color: #E2E8F0; }
        section { padding-top: 100px; padding-bottom: 100px; }
        h1, h2, h3 { font-weight: 900; letter-spacing: -0.04em; color: #F8FAFC; }
    </style>
</head>
<body class="bg-dark-bg font-sans antialiased">
    
    <%- include('partials/header') %>

    <section class="py-24 bg-gray-900 border-b border-dark-card">
        <div class="container mx-auto px-4 text-center">
            <h1 class="text-5xl lg:text-7xl font-black mb-4 text-white" data-aos="fade-down">
                Server Location
            </h1>
            <p class="text-xl text-gray-400 max-w-2xl mx-auto" data-aos="fade-down" data-aos-delay="100">
                Our primary datacenter is strategically located in Virginia for optimal performance across North America.
            </p>
        </div>
    </section>

    <section class="py-20 bg-dark-bg">
        <div class="container mx-auto px-4">
            <div class="max-w-4xl mx-auto">
                <!-- Main Location Card -->
                <div class="bg-dark-card rounded-2xl shadow-2xl border-2 border-rivix-orange/50 overflow-hidden" data-aos="fade-up">
                    <div class="bg-gradient-to-r from-rivix-orange/20 to-rivix-orange/10 p-8 border-b border-rivix-orange/30">
                        <div class="flex items-center justify-between flex-wrap gap-4">
                            <div>
                                <div class="flex items-center gap-3 mb-2">
                                    <div class="w-16 h-16 bg-rivix-orange rounded-xl flex items-center justify-center shadow-lg">
                                        <i class="fas fa-server text-white text-2xl"></i>
                                    </div>
                                    <div>
                                        <h2 class="text-3xl font-black text-white"><%= data.location.name %></h2>
                                        <p class="text-gray-300 text-lg"><%= data.location.location %></p>
                                    </div>
                                </div>
                            </div>
                            <div class="flex items-center gap-2 px-4 py-2 bg-rivix-green/20 border border-rivix-green/50 rounded-lg">
                                <div class="w-3 h-3 bg-rivix-green rounded-full animate-pulse"></div>
                                <span class="text-rivix-green font-bold text-sm uppercase">Operational</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="p-8">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                            <div class="bg-dark-bg/50 rounded-xl p-6 border border-gray-700">
                                <div class="flex items-center gap-3 mb-4">
                                    <div class="w-12 h-12 bg-rivix-orange/20 rounded-lg flex items-center justify-center">
                                        <i class="fas fa-map-marker-alt text-rivix-orange text-xl"></i>
                                    </div>
                                    <h3 class="text-xl font-bold text-white">Datacenter</h3>
                                </div>
                                <p class="text-gray-300 text-lg"><%= data.location.datacenter %></p>
                                <p class="text-gray-400 text-sm mt-2"><%= data.location.location %></p>
                            </div>
                            
                            <div class="bg-dark-bg/50 rounded-xl p-6 border border-gray-700">
                                <div class="flex items-center gap-3 mb-4">
                                    <div class="w-12 h-12 bg-rivix-blue/20 rounded-lg flex items-center justify-center">
                                        <i class="fas fa-satellite-dish text-rivix-blue text-xl"></i>
                                    </div>
                                    <h3 class="text-xl font-bold text-white">Average Latency</h3>
                                </div>
                                <div class="flex items-baseline gap-2">
                                    <span class="text-4xl font-black text-rivix-green" id="ping-display"><%= data.location.latency %></span>
                                    <span class="text-gray-400 text-lg">ms</span>
                                </div>
                                <button onclick="testPing()" class="mt-4 w-full py-2 px-4 bg-rivix-orange/10 hover:bg-rivix-orange/20 text-rivix-orange text-sm font-semibold rounded-lg transition-colors border border-rivix-orange/30">
                                    <i class="fas fa-satellite-dish mr-2"></i>Test Ping Now
                                </button>
                            </div>
                        </div>
                        
                        <div class="bg-gradient-to-br from-gray-800/50 to-dark-bg/50 rounded-xl p-6 border border-gray-700">
                            <h3 class="text-xl font-bold text-white mb-4 flex items-center gap-2">
                                <i class="fas fa-info-circle text-rivix-orange"></i>
                                Why This Location?
                            </h3>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-gray-300">
                                <div class="flex items-start gap-3">
                                    <i class="fas fa-check-circle text-rivix-green mt-1"></i>
                                    <span>Optimal routing for North American players</span>
                                </div>
                                <div class="flex items-start gap-3">
                                    <i class="fas fa-check-circle text-rivix-green mt-1"></i>
                                    <span>Tier 1 network providers</span>
                                </div>
                                <div class="flex items-start gap-3">
                                    <i class="fas fa-check-circle text-rivix-green mt-1"></i>
                                    <span>Enterprise-grade infrastructure</span>
                                </div>
                                <div class="flex items-start gap-3">
                                    <i class="fas fa-check-circle text-rivix-green mt-1"></i>
                                    <span>Low latency to major US cities</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Performance Metrics -->
                <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-6" data-aos="fade-up" data-aos-delay="200">
                    <div class="bg-dark-card rounded-xl p-6 border border-gray-700 text-center">
                        <div class="w-16 h-16 mx-auto mb-4 bg-rivix-green/20 rounded-xl flex items-center justify-center">
                            <i class="fas fa-tachometer-alt text-rivix-green text-2xl"></i>
                        </div>
                        <div class="text-3xl font-black text-white mb-2">< 15ms</div>
                        <div class="text-gray-400">Average Latency</div>
                    </div>
                    
                    <div class="bg-dark-card rounded-xl p-6 border border-gray-700 text-center">
                        <div class="w-16 h-16 mx-auto mb-4 bg-rivix-blue/20 rounded-xl flex items-center justify-center">
                            <i class="fas fa-network-wired text-rivix-blue text-2xl"></i>
                        </div>
                        <div class="text-3xl font-black text-white mb-2">99.99%</div>
                        <div class="text-gray-400">Uptime SLA</div>
                    </div>
                    
                    <div class="bg-dark-card rounded-xl p-6 border border-gray-700 text-center">
                        <div class="w-16 h-16 mx-auto mb-4 bg-rivix-orange/20 rounded-xl flex items-center justify-center">
                            <i class="fas fa-shield-alt text-rivix-orange text-2xl"></i>
                        </div>
                        <div class="text-3xl font-black text-white mb-2">1Tbps</div>
                        <div class="text-gray-400">DDoS Protection</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include('partials/footer') %>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({
            duration: 800,
            easing: 'ease',
            once: true
        });
        
        // Ping Test Functionality
        function testPing() {
            const pingDisplay = document.getElementById('ping-display');
            const originalText = pingDisplay.textContent;
            const originalClass = pingDisplay.className;
            
            // Simulate ping test
            pingDisplay.textContent = 'Testing...';
            pingDisplay.className = 'text-4xl font-black text-gray-400';
            
            // Simulate network delay with slight variation
            setTimeout(() => {
                const baseLatency = <%= data.location.latency %>;
                const variation = Math.floor(Math.random() * 10) - 5; // -5 to +5ms variation
                const actualLatency = Math.max(1, baseLatency + variation);
                
                pingDisplay.textContent = actualLatency;
                pingDisplay.className = actualLatency < 20 ? 'text-4xl font-black text-rivix-green' : 'text-4xl font-black text-rivix-orange';
                
                // Reset after 3 seconds
                setTimeout(() => {
                    pingDisplay.textContent = originalText;
                    pingDisplay.className = originalClass;
                }, 3000);
            }, 800 + Math.random() * 400); // 800-1200ms delay
        }
    </script>
</body>
</html>
